nacos.server-addr: 127.0.0.1:8848  #自己
nacos.namespace: 07883370-2b5e-4992-81ef-0eaad270c43b

server:
  port: 9991
spring:
  application:
   name: auth
  cloud:
    nacos:
      config:
        server-addr: ${nacos.server-addr}
        namespace: ${nacos.namespace}
      discovery:
        server-addr: ${nacos.server-addr}
        namespace: ${nacos.namespace}
  redis:
    host: 127.0.0.1
    database: 0
    password:
    port: 6379
    jedis:
      pool:
        max-active: 8
        max-wait: 3
        max-idle: 8
        min-idle: 0
oauth:
  validity:
    accessToken: 36000
    refreshToken: 36000